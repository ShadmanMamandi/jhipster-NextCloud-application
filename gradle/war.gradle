apply plugin: "war"

bootWar {
    mainClassName = "com.magicbox.myapp.JhipsterNextCloudApplicationApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
